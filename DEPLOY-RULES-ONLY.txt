هذه النسخة لا تحتوي على Firebase Functions.

لتفعيل صور الوجبات وتسعير الوحدات وتحويل التوصيل إلى كابتن داخل النطاق: انشر firestore.rules وstorage.rules أولًا، ثم ارفع ملفات الواجهة إلى الاستضافة.

الأمر لنشر القواعد فقط:
firebase deploy --only firestore:rules,storage
