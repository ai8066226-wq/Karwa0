هذه النسخة لا تحتوي على Firebase Functions.

لتفعيل تسعير الوحدات وتحويل التوصيل إلى كابتن داخل النطاق: انشر firestore.rules أولًا، ثم ارفع ملفات الواجهة إلى الاستضافة. رفع صور الوجبات غير موجود في هذه النسخة.

الأمر لنشر القواعد فقط:
firebase deploy --only firestore:rules
